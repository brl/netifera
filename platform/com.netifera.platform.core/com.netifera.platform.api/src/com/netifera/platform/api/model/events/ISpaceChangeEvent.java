package com.netifera.platform.api.model.events;

/*
 * Fired whenever a Space changes, i.e. entities are added or removed form it,
 * tasks are created or terminated, the space is renamed, etc
 */
public interface ISpaceChangeEvent extends ISpaceEvent {
}
